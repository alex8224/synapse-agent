name: Build Native Compression Wheels

on:
  push:
    tags:
      - "synapse-tool-compress-core-v*"
  pull_request:
    paths:
      - "rust/synapse-tool-compress-core/**"
      - ".github/workflows/native-compression-wheels.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  wheel:
    name: ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
    steps:
      - uses: actions/checkout@v4
      - uses: PyO3/maturin-action@v1
        with:
          command: build
          working-directory: rust/synapse-tool-compress-core
          target: ${{ matrix.target }}
          args: --release --locked --compatibility pypi --out dist
          manylinux: 2014
          # tree-sitter 0.25 requires _BSD_SOURCE on manylinux2014's glibc 2.17
          # for le16toh/be16toh to expand as macros instead of unresolved symbols.
          docker-options: --env CFLAGS=-D_BSD_SOURCE
      - name: Smoke test wheel
        shell: bash
        run: |
          python -m pip install --force-reinstall rust/synapse-tool-compress-core/dist/*.whl
          python -c "import synapse_tool_compress_core; print('native wheel import OK')"
      - uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.target }}
          path: rust/synapse-tool-compress-core/dist/*.whl

  publish:
    if: startsWith(github.ref, 'refs/tags/synapse-tool-compress-core-v')
    needs: wheel
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: wheel-*
          merge-multiple: true
          path: dist
      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*.whl
          name: synapse-tool-compress-core ${{ github.ref_name }}
